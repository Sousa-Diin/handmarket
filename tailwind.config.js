export default {
  theme: {
    extend: {
      colors: {
        primary: 'var(--color-primary)',      // Fundo preto dos cards
        secondary: 'var(--color-secondary)',   // Fundo geral cinza escuro
        tertiary: 'var(--color-tertiary)',      // Destaques e divisões
        accent: '#f97316',          // Laranja vibrante (botões, ênfases)
        info: 'var(--color-info)',              // Verde (valores positivos, status OK)
        highlight: 'var(--color-highlight)',    // Roxo usado em gráficos
        warning: 'var(--color-warning)',        // Amarelo de atenção
        danger: 'var(--color-danger)',          // Vermelho de alerta/erro
        light: 'var(--color-light)',            // Texto e detalhes claros
      },
    },
  },
};
